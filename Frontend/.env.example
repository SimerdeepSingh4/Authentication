# No frontend environment variables are required right now.
# The frontend is configured to call the backend with a relative API path.
# Example only if you later switch to env-driven config:
#
# VITE_API_BASE_URL=/api/auth
